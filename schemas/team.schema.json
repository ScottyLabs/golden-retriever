{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "team.schema.json",
  "title": "Team",
  "description": "Schema for a team registration file. Extended fields mirror ScottyLabs governance synchronizer behavior; see docs/synchronizer-model.md.",
  "type": "object",
  "required": [
    "name",
    "slug",
    "maintainers",
    "contributors"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "Human-readable team name."
    },
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9]+([a-z0-9-]*[a-z0-9])?$",
      "description": "URL-safe identifier. Must match the filename (without .json)."
    },
    "description": {
      "type": "string",
      "description": "A brief description of the team or project."
    },
    "maintainers": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "uniqueItems": true,
      "description": "Contributor filenames (without .json) who are maintainers. Maintainers receive elevated repository permissions and team management rights."
    },
    "contributors": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "description": "Contributor filenames (without .json) who are contributors. All maintainers should also appear here."
    },
    "repos": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9]+([a-z0-9-]*[a-z0-9])?$"
      },
      "uniqueItems": true,
      "description": "Repository filenames (without .json) from the repos/ directory. Platform-specific details are defined in each repository file."
    },
    "figma_projects": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9]+([a-z0-9-]*[a-z0-9])?$"
      },
      "uniqueItems": true,
      "description": "Figma project filenames (without .json) from the figma-projects/ directory."
    },
    "google_files": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9]+([a-z0-9-]*[a-z0-9])?$"
      },
      "uniqueItems": true,
      "description": "Google file slugs (without .json) from the google-files/ directory."
    },
    "create_oidc_clients": {
      "type": "boolean",
      "description": "When true (default if omitted), a synchronizer creates Keycloak OIDC clients for local/dev/staging/prod. Set false to opt out (governance: create-oidc-clients)."
    },
    "website_slug": {
      "type": "string",
      "pattern": "^[a-z0-9]+([a-z0-9-]*[a-z0-9])?$",
      "description": "Short site key for default OIDC redirect URI templates (api.<website_slug>.scottylabs.org, etc.). Should be set when create_oidc_clients is true."
    },
    "ext_admins": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "description": "Contributor slugs (without .json) placed in the team's Keycloak/Vault external-admins group when present (governance: ext-admins)."
    },
    "applicants": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "description": "Contributor slugs for the applicants group (Keycloak/Vault applicants path) when present (governance: applicants)."
    },
    "secrets_population_layout": {
      "type": "string",
      "enum": [
        "none",
        "single",
        "multi"
      ],
      "description": "HashiCorp Vault secrets folder layout for the team (governance: secrets-population-layout). Omit to follow synchronizer default."
    },
    "slack_channels": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true,
      "description": "Slack channel names (or IDs) for synchronizer invites (governance Slack section)."
    },
    "sync_github": {
      "type": "boolean",
      "default": true,
      "description": "When true, automatically sync team membership and repo access to GitHub."
    },
    "sync_forgejo": {
      "type": "boolean",
      "default": false,
      "description": "When true, automatically sync team membership and repo access to Forgejo/Codeberg."
    },
    "remove_unlisted": {
      "type": "boolean",
      "default": true,
      "description": "When true, members not listed in this file will be removed from the platform team. Set to false during migration periods."
    }
  },
  "additionalProperties": false
}
