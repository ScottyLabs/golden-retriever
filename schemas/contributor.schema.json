{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "contributor.schema.json",
  "title": "Contributor",
  "description": "Schema for a contributor registration file.",
  "type": "object",
  "required": [
    "full_name"
  ],
  "properties": {
    "full_name": {
      "type": "string",
      "description": "The contributor's full display name."
    },
    "github_username": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9]([a-zA-Z0-9-]*[a-zA-Z0-9])?$",
      "description": "GitHub username. Required if the contributor participates in any GitHub-synced team."
    },
    "forgejo_username": {
      "type": "string",
      "description": "Forgejo/Codeberg username. Required if the contributor participates in any Forgejo-synced team."
    }
  },
  "additionalProperties": false
}
